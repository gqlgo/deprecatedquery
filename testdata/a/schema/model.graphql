type Type {
	field: String!
	deprecatedField: String! @deprecated(reason: "use field instead")
}

type DeprecatedType {
	field: String!
	deprecatedField: String! @deprecated(reason: "use field instead")
}

enum Enum {
	VALUE
	DEPRECATED_VALUE @deprecated(reason: "use VALUE instead")
}

